---
phase: 04-audit-ready-outputs
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/app.py
  - scripts/test_runtime_validation.py
autonomous: true
requirements:
  - EXPT-01
  - EXPT-03
user_setup: []
must_haves:
  truths:
    - "Latest usable raw and review artifacts can be reused for debugging or re-export without rerunning a full scrape."
    - "Cached, stale, and fallback raw-source semantics survive beyond the live response and remain available to export flows."
    - "Final-review export can fall back to backend-saved review artifacts when the frontend payload is missing or stale."
    - "Artifact reuse stays lightweight and file-based instead of introducing a new workflow stage or heavy audit subsystem."
  artifacts:
    - path: "backend/app.py"
      provides: "Lightweight saved raw/review/visual artifact metadata and reuse helpers for export routes"
      contains: "load_latest_usable_scrape_artifact"
    - path: "scripts/test_runtime_validation.py"
      provides: "Local checks for snapshot reuse, stale-result export fallback, and backend-side final-review recovery"
      contains: "stale_result"
  key_links:
    - from: "backend/app.py"
      to: "backend/app.py"
      via: "Persist and reload latest usable review/visual artifacts from `data/<platform>/` for re-export"
      pattern: "save_profile_reviews|load_profile_reviews|perform_visual_review|download_final_review"
    - from: "backend/app.py"
      to: "scripts/test_runtime_validation.py"
      via: "Fallback export assertions for cached, stale, and last_non_empty_snapshot cases"
      pattern: "last_non_empty_snapshot|stale_result|final-review"
---

<objective>
Improve artifact retention and snapshot reuse so operators and maintainers can re-export or debug from the latest usable raw/review state without rerunning the full workflow.

Purpose: Preserve auditability when scrape output is cached, stale, partially failed, or when final-review export needs backend-saved artifacts rather than a fragile frontend copy.
Output: Lightweight backend artifact metadata, saved review/visual snapshots under existing platform data directories, and regression checks proving export fallback behavior.
</objective>

<execution_context>
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audit-ready-outputs/04-CONTEXT.md
@backend/app.py
@scripts/test_runtime_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist lightweight raw/review snapshot metadata for later export reuse</name>
  <files>backend/app.py</files>
  <read_first>backend/app.py, .planning/phases/04-audit-ready-outputs/04-CONTEXT.md, .planning/PROJECT.md</read_first>
  <action>In `backend/app.py`, extend the existing file-based artifact model with lightweight sibling metadata/helpers instead of a new subsystem. Reuse `data/<platform>/` as the storage surface. Add exact helper paths for latest usable review and visual-review artifacts, for example saved `profile_reviews` context plus saved `visual_results` / `review_history` / `live_review` snapshots from `perform_visual_review()`. Persist enough metadata to recover `cached`, `stale_result`, `used_fallback`, `raw_data_source`, source path, and updated-at values for later exports. Update export-facing helpers and routes so `download_test_info`, raw export reuse, review exports, and `download_final_review` can recover from backend-saved artifacts when current raw files are empty or request payloads are stale/missing. Preserve existing routes and file-based storage.</action>
  <verify>PYTHONPYCACHEPREFIX=/tmp backend/.venv/bin/python -m py_compile backend/app.py</verify>
  <acceptance_criteria>
    - `backend/app.py` defines explicit helper paths for latest usable review or visual-review artifacts under `data/<platform>/`
    - saved artifact metadata includes exact keys `cached`, `stale_result`, `used_fallback`, `raw_data_source`, and `updated_at`
    - `download_final_review` can recover backend-saved artifacts when request payload fields are absent or stale
    - raw/test export reuse continues to prefer `current` raw data and fall back to `last_non_empty_snapshot` without new workflow stages
    - no database, queue, or separate audit service is introduced
  </acceptance_criteria>
  <done>Latest usable artifacts are recoverable from backend-side files instead of only from the current in-memory response.</done>
</task>

<task type="auto">
  <name>Task 2: Add fallback-focused regression coverage for snapshot reuse and re-export</name>
  <files>scripts/test_runtime_validation.py</files>
  <read_first>scripts/test_runtime_validation.py, backend/app.py</read_first>
  <action>Add local tests that write minimal raw, review, and visual-review artifacts into temporary `data/<platform>/` paths, then exercise the backend reuse behavior. Cover at least: `last_non_empty_snapshot` raw fallback for test/debug export, stale-result review fallback, and `POST /api/download/<platform>/final-review` recovering final-review rows from backend-saved artifacts when the request body omits or stales one of `profile_reviews` / `visual_results`. Assert exact response codes, exact fallback keys such as `raw_data_source` and `stale_result`, and at least one saved visual-review artifact filename or helper path. Keep the checks fully local.</action>
  <verify>PYTHONPYCACHEPREFIX=/tmp backend/venv/bin/python scripts/test_runtime_validation.py</verify>
  <acceptance_criteria>
    - validation covers `last_non_empty_snapshot` reuse for raw/test export flows
    - validation covers stale-result reuse for review exports
    - validation covers backend fallback in `download_final_review` when request payload data is missing or stale
    - validation asserts exact keys `raw_data_source` and `stale_result`
    - no network or external provider access is introduced
  </acceptance_criteria>
  <done>Artifact reuse and re-export fallback behavior are protected by repeatable local checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPYCACHEPREFIX=/tmp backend/.venv/bin/python -m py_compile backend/app.py`
- [ ] `PYTHONPYCACHEPREFIX=/tmp backend/venv/bin/python scripts/test_runtime_validation.py`
- [ ] latest usable raw/review artifacts can be recovered from `data/<platform>/` without rerunning scrape
- [ ] final-review export can fall back to backend-saved artifacts when frontend payload data is stale or missing
</verification>

<success_criteria>

- All tasks completed
- Verification checks pass
- Snapshot reuse remains file-based and lightweight
- Re-export/debug flows no longer depend on a fresh scrape or a pristine frontend payload copy

</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-ready-outputs/04-02-SUMMARY.md`
</output>
