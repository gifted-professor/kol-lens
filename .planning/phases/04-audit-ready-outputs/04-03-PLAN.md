---
phase: 04-audit-ready-outputs
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - frontend/src/App.jsx
  - config/field_mapping.json
  - docs/field_dictionary.md
  - docs/prd/master_vetting_sop.md
  - docs/prd/instagram_prd_mapping.md
  - docs/prd/tiktok_prd_mapping.md
autonomous: true
requirements:
  - OPER-01
  - OPER-02
user_setup: []
must_haves:
  truths:
    - "Operator can tell whether the UI is showing partial, cached, stale, fallback, or completed results and what action is available next."
    - "Existing export buttons remain in place, but their grouping and availability make workflow intent obvious."
    - "Maintainer can trace active field usage and screening-rule references from docs/config without reverse-engineering backend and screening code."
    - "Field/rule traceability remains maintainer-facing in repo docs/config rather than becoming a new operator-facing browser."
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Action-oriented status copy, export grouping, and next-step guidance for existing workflow states"
      contains: "downloadFinalReview"
    - path: "config/field_mapping.json"
      provides: "Canonical machine-readable field/rule traceability source aligned with current screening flows"
      contains: "\"upload_metadata\""
    - path: "docs/field_dictionary.md"
      provides: "Generated maintainer-facing field inventory aligned with current mapping config"
      contains: "字段字典"
    - path: "docs/prd/master_vetting_sop.md"
      provides: "Workflow-level explanation of active screening paths and export handoff expectations"
      contains: "当前主链路"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "backend/app.py"
      via: "UI state copy and next-action affordances must reflect `cached`, `stale_result`, `used_fallback`, `is_partial`, and final-review readiness"
      pattern: "cached|stale_result|used_fallback|is_partial|visual_results"
    - from: "config/field_mapping.json"
      to: "docs/field_dictionary.md"
      via: "Regenerated field dictionary from the canonical machine-readable mapping"
      pattern: "build_field_dictionary.py"
    - from: "docs/prd/master_vetting_sop.md"
      to: "docs/prd/instagram_prd_mapping.md"
      via: "Shared maintainer traceability for active screening functions and field sources"
      pattern: "check_instagram_custom|check_tiktok_tapo|field_dictionary"
---

<objective>
Refresh UI status guidance and maintainer traceability docs so operator expectations and repo source-of-truth stay aligned with the hardened export behavior.

Purpose: Make the tool self-explanatory during live workflows while keeping field/rule traceability in docs/config rather than adding a new UI subsystem.
Output: Clear result-state and next-step guidance in the existing frontend surface, plus synchronized config/docs for field and screening-rule traceability.
</objective>

<execution_context>
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audit-ready-outputs/04-CONTEXT.md
@frontend/src/App.jsx
@backend/app.py
@config/field_mapping.json
@docs/field_dictionary.md
@docs/prd/master_vetting_sop.md
@docs/prd/instagram_prd_mapping.md
@docs/prd/tiktok_prd_mapping.md
@scripts/build_field_dictionary.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarify result-state and next-action guidance in the existing export surface</name>
  <files>frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, backend/app.py, .planning/phases/04-audit-ready-outputs/04-CONTEXT.md</read_first>
  <action>In `frontend/src/App.jsx`, keep the current single-screen workflow and existing export actions, but make their purpose and readiness state explicit. Group the current buttons by workflow purpose (debug/spot-check versus review handoff), and add action-oriented copy tied to the real backend flags `cached`, `stale_result`, `used_fallback`, `is_partial`, visual-review readiness, and final-review readiness. The fallback guidance must key off the actual backend fallback signal (`used_fallback`, or the exact chosen equivalent if renamed in implementation) rather than only inferred copy. When scrape completes with eligible pass profiles, explicitly point to visual review as the next step. When visual review completes, explicitly indicate final-review export as the next step. When showing cached, stale, fallback, or partial results, explain what the operator is seeing and which action is available now. Do not invent a new export center or workflow stage.</action>
  <verify>cd frontend && npm run lint && npm run build</verify>
  <acceptance_criteria>
    - `frontend/src/App.jsx` contains explicit copy branches for `cached`, `stale_result`, `used_fallback`, and `is_partial`
    - fallback UI guidance is wired to the backend fallback signal `used_fallback` (or the exact chosen equivalent signal), not only inferred from generic cached wording
    - existing export actions remain present and are grouped by workflow purpose rather than replaced
    - UI copy names visual review as the next action when eligible pass profiles exist
    - UI copy names final-review export as the next action after visual review completion
    - no new page, export hub, or field/rule explorer UI is added
  </acceptance_criteria>
  <done>The operator can understand current state and next action directly from the existing screen.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh maintainer-facing field and screening-rule traceability docs/config</name>
  <files>config/field_mapping.json, docs/field_dictionary.md, docs/prd/master_vetting_sop.md, docs/prd/instagram_prd_mapping.md, docs/prd/tiktok_prd_mapping.md</files>
  <read_first>config/field_mapping.json, docs/field_dictionary.md, docs/prd/master_vetting_sop.md, docs/prd/instagram_prd_mapping.md, docs/prd/tiktok_prd_mapping.md, scripts/build_field_dictionary.py, .planning/phases/04-audit-ready-outputs/04-CONTEXT.md</read_first>
  <action>Update the canonical maintainer traceability surface without expanding UI scope. In `config/field_mapping.json`, refresh version/meta notes and any active-flow references needed to match the current screening implementation and Phase 4 audit expectations. Regenerate `docs/field_dictionary.md` via `scripts/build_field_dictionary.py`. Then update `docs/prd/master_vetting_sop.md`, `docs/prd/instagram_prd_mapping.md`, and `docs/prd/tiktok_prd_mapping.md` so they explicitly point maintainers to the active screening entry functions, the field dictionary/config source-of-truth, and the audit/export handoff expectations that Phase 4 hardens. Keep this maintainer-facing and concise.</action>
  <verify>backend/.venv/bin/python scripts/build_field_dictionary.py</verify>
  <acceptance_criteria>
    - `config/field_mapping.json` has an updated Phase 4-aligned version or note entry
    - `docs/field_dictionary.md` is regenerated from `scripts/build_field_dictionary.py`
    - `docs/prd/master_vetting_sop.md`, `docs/prd/instagram_prd_mapping.md`, and `docs/prd/tiktok_prd_mapping.md` point to the canonical field dictionary/config surfaces
    - PRD docs explicitly name the active screening functions now in use
    - docs remain maintainer-facing and do not introduce an operator UI requirement
  </acceptance_criteria>
  <done>Maintainers can trace active fields and rule paths from docs/config instead of spelunking code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
- [ ] `backend/.venv/bin/python scripts/build_field_dictionary.py`
- [ ] UI copy distinguishes partial, cached, stale, fallback, and completed states with an explicit next action
- [ ] PRD docs and generated field dictionary point to the same canonical mapping/config surfaces
</verification>

<success_criteria>

- All tasks completed
- Verification checks pass
- Operator-facing status and next-step guidance match real backend workflow states
- Maintainer-facing field/rule traceability is visible from docs/config without adding a new subsystem

</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-ready-outputs/04-03-SUMMARY.md`
</output>
