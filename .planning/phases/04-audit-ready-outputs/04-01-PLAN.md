---
phase: 04-audit-ready-outputs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app.py
  - scripts/test_runtime_validation.py
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
user_setup: []
must_haves:
  truths:
    - "Operator can download the existing raw JSON route (`/api/download/<platform>/json`), raw Excel route (`/api/download/<platform>/excel`), test-info, prescreen-review, image-review, and final-review outputs without guessing which file contains the audit keys."
    - "Review-oriented exports expose stable identifiers, reasons, profile URLs, and upload-derived metadata needed for manual follow-up."
    - "Test/debug exports and review handoff exports use consistent identity and provenance column names instead of drifting by route."
    - "Final review rows can be matched back to prescreen rows and uploaded workbook context by one stable identifier."
  artifacts:
    - path: "backend/app.py"
      provides: "Shared export-row identity and provenance helpers reused by review and test export builders"
      contains: "build_prescreen_review_rows"
    - path: "scripts/test_runtime_validation.py"
      provides: "Repeatable checks that exported rows and download routes include the required audit columns"
      contains: "final_review"
  key_links:
    - from: "backend/app.py"
      to: "scripts/test_runtime_validation.py"
      via: "Route-level export contract assertions for raw JSON, raw Excel, test-info, prescreen-review, image-review, and final-review outputs"
      pattern: "identifier|profile_url|source_filename|reason|raw_data_source"
    - from: "backend/app.py"
      to: "backend/app.py"
      via: "Raw exports remain the existing `download_results(..., format)` routes while audit-oriented exports must use shared row builders without route-specific drift"
      pattern: "download_results|download_test_info|download_prescreen_review|download_image_review|download_final_review"
---

<objective>
Unify export row contracts so all supported raw/test/review outputs expose the availability and audit fields operators need without route-specific gaps.

Purpose: Make Phase 4 exports trustworthy for manual audit and follow-up while preserving the existing export entry points, including the existing raw JSON and raw Excel routes.
Output: Shared backend export-row helpers, explicit coverage for raw JSON/raw Excel plus test/review download routes, and repeatable regression checks for required audit columns.
</objective>

<execution_context>
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-audit-ready-outputs/04-CONTEXT.md
@.planning/phases/03-screening-visual-review-consistency/03-03-SUMMARY.md
@backend/app.py
@scripts/test_runtime_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize export identity and provenance columns across review/test outputs</name>
  <files>backend/app.py</files>
  <read_first>backend/app.py, .planning/phases/04-audit-ready-outputs/04-CONTEXT.md, .planning/phases/03-screening-visual-review-consistency/03-03-SUMMARY.md</read_first>
  <action>In `backend/app.py`, keep raw exports explicit as the existing `download_results(platform, format)` routes where raw JSON means `GET /api/download/<platform>/json` and raw Excel means `GET /api/download/<platform>/excel`. Then add one shared helper path for audit-ready export columns and apply it to `build_prescreen_review_rows()`, `build_image_review_rows()`, `build_final_review_rows()`, `build_test_info_summary_rows()`, `build_test_info_raw_rows()`, and `build_test_info_json_payload()`. Preserve the current raw/test/review route names, but make the exported contracts converge on explicit fields for stable identity and audit tracing: `platform`, `identifier`, `username` or `account_id` as needed, `profile_url`, review status/reason fields, `source_filename`, and raw-source fields where they already exist. Do not redesign exports into a new hub; preserve current file purposes while eliminating silent column drift between routes.</action>
  <verify>backend/.venv/bin/python -m py_compile backend/app.py</verify>
  <acceptance_criteria>
    - `backend/app.py` contains one shared helper path that appends or derives audit-ready identity/provenance fields for export builders
    - raw JSON remains the existing `GET /api/download/<platform>/json` route and raw Excel remains the existing `GET /api/download/<platform>/excel` route
    - `build_prescreen_review_rows`, `build_image_review_rows`, `build_final_review_rows`, `build_test_info_summary_rows`, and `build_test_info_raw_rows` all emit `profile_url`
    - review-oriented export rows expose `identifier` plus status/reason fields without relying on route-specific ad hoc names only
    - `source_filename` remains traceable in exported rows via upload metadata columns
    - no new export route or workflow stage is introduced
  </acceptance_criteria>
  <done>Export builders share one audit contract instead of drifting by route.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the export contract with route-level regression checks</name>
  <files>scripts/test_runtime_validation.py</files>
  <read_first>scripts/test_runtime_validation.py, backend/app.py</read_first>
  <action>Add focused validation that exercises the existing Flask download paths for raw JSON (`/api/download/<platform>/json`), raw Excel (`/api/download/<platform>/excel`), `test-info`, `test-info-json`, `prescreen-review`, `image-review`, and `final-review`. Use the test client plus `openpyxl`/JSON parsing to assert route availability plus required headers or keys such as `identifier`, `profile_url`, `source_filename`, `raw_data_source`, and the relevant reason/status columns. State plainly in the assertions that raw exports are the `download_results(..., format)` routes, while `test-info-json` remains the structured debug/test JSON export. Cover at least one cross-check showing the same identifier can be traced from review-oriented exports back to upload metadata fields. Keep the checks local-only and avoid network-backed flows.</action>
  <verify>backend/.venv/bin/python scripts/test_runtime_validation.py</verify>
  <acceptance_criteria>
    - validation code hits the Flask download routes for raw JSON, raw Excel, `test-info`, `test-info-json`, `prescreen-review`, `image-review`, and `final-review`
    - validation states plainly that raw exports are the existing `download_results(..., format)` routes
    - validation asserts exact header/key strings including `identifier` and `profile_url`
    - validation asserts upload-metadata-derived provenance remains visible through `source_filename` or equivalent exported metadata columns
    - validation remains local-only and does not call external providers
    - existing runtime validation coverage continues to pass
  </acceptance_criteria>
  <done>Audit column coverage is protected by repeatable local checks instead of manual spot inspection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend/.venv/bin/python -m py_compile backend/app.py`
- [ ] `backend/.venv/bin/python scripts/test_runtime_validation.py`
- [ ] raw JSON, raw Excel, `test-info`, `prescreen-review`, `image-review`, and `final-review` routes are all covered explicitly
- [ ] review/test exports expose stable identity fields that can be grep-checked in tests
- [ ] route-level export coverage proves operators can download the supported raw/test/review output families without missing audit columns
</verification>

<success_criteria>

- All tasks completed
- Verification checks pass
- Existing export routes remain intact
- Review/test exports now expose a consistent audit-ready identity and provenance contract

</success_criteria>

<output>
After completion, create `.planning/phases/04-audit-ready-outputs/04-01-SUMMARY.md`
</output>
