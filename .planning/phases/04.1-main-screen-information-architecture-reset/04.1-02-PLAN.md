---
phase: 04.1-main-screen-information-architecture-reset
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - frontend/src/App.jsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "The post-run workspace is split into explicit ownership boundaries for overview, profile cards, visual review, and export handoff."
    - "The same metric or status appears only once per workspace, and `Next Action` no longer competes with the export wall, visual-review queue, and run snapshot in one region."
    - "Visual-review controls and history stay with the visual-review desk, while export actions and run snapshot move into an export handoff surface."
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Strict results-workspace tabs with dedicated ownership boundaries"
      contains: "导出交接"
    - path: "frontend/src/App.jsx"
      provides: "A dedicated visual-review workspace instead of an inline append-only block"
      contains: "Visual Review Desk"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "backend/app.py"
      via: "Results workspace states must still surface backend-driven flags like `cached`, `stale_result`, `used_fallback`, `is_partial`, and final-review artifact availability."
      pattern: "cached|stale_result|used_fallback|is_partial|savedFinalReviewArtifactsAvailable|handleVisualReview|exportActionGroups|Run Snapshot"
---

<objective>
Recompose the post-run workspace so every downstream surface has an explicit owner instead of sharing one append-only overview region.

Purpose: Convert the current result shell into a bounded results workspace that later modularization can safely split by boundary rather than by arbitrary vertical position.
Output: Strict results tabs for overview, profile cards, visual review, and export handoff, with duplicated summary blocks removed.
</objective>

<execution_context>
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md
@frontend/src/App.jsx
@backend/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the partial result toggle with strict results-workspace tabs</name>
  <files>frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, .planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md, backend/app.py</read_first>
  <action>In `frontend/src/App.jsx`, replace the current radio-input result toggle (`result-view-overview` / `result-view-profiles`) with an explicit results tab state using the concrete values `overview`, `profiles`, `visual-review`, and `export-handoff`. Render tab labels exactly as `概览`, `博主卡片`, `视觉复核`, and `导出交接`. Keep one canonical results header that surfaces backend-driven state chips such as `cached`, `stale_result`, `used_fallback`, `is_partial`, and final-review artifact availability, but stop duplicating the same count/status cards again inside multiple result subviews. The `概览` tab must keep only canonical summary metrics plus one `Next Action` block. The `博主卡片` tab must own the existing `renderProfileCards()` output and its empty state.</action>
  <verify>cd frontend && npm run lint && npm run build</verify>
  <acceptance_criteria>
    - `frontend/src/App.jsx` contains the exact tab labels `概览`, `博主卡片`, `视觉复核`, and `导出交接`
    - `frontend/src/App.jsx` contains a results-tab state with concrete values `overview`, `profiles`, `visual-review`, and `export-handoff`
    - `frontend/src/App.jsx` no longer contains `result-view-overview`, `result-view-profiles`, `peer/result-overview`, or `peer/result-profiles`
    - `renderProfileCards()` is rendered only from the `profiles` tab branch
    - `frontend/src/App.jsx` contains both `activeResultTab === 'overview'` and `activeResultTab === 'profiles'`
  </acceptance_criteria>
  <done>The results workspace has strict navigation and overview stops acting like a catch-all region.</done>
</task>

<task type="auto">
  <name>Task 2: Move visual review and export handoff into their own owned surfaces</name>
  <files>frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, backend/app.py, .planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md</read_first>
  <action>Move the existing visual-review mode selector, `Visual Review Desk`, live/history navigation, completed review queue, and `handleVisualReview` CTA into the `visual-review` tab so they stop living inline above the overview tab. Move the `exportActionGroups` block and the current `Run Snapshot` card into the `export-handoff` tab. Keep `Run Snapshot` secondary by placing it below export actions instead of beside the main call to action. Keep final-review readiness driven by the existing backend flags and current client state, but present that readiness only in the relevant tab rather than competing across overview, next action, and export cards at the same time.</action>
  <verify>cd frontend && npm run lint && npm run build</verify>
  <acceptance_criteria>
    - `frontend/src/App.jsx` contains both `activeResultTab === 'visual-review'` and `activeResultTab === 'export-handoff'`
    - `frontend/src/App.jsx` renders `Visual Review Desk` only from the `visual-review` tab branch
    - `frontend/src/App.jsx` renders `exportActionGroups.map` only from the `export-handoff` tab branch
    - `frontend/src/App.jsx` renders `Run Snapshot` inside the `export-handoff` tab branch and below export actions
    - `frontend/src/App.jsx` keeps the existing backend-driven readiness signals for visual review and final-review export
  </acceptance_criteria>
  <done>Post-run surfaces now have clear ownership boundaries that Phase 5 can modularize against.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
- [ ] results tabs exist for `概览`, `博主卡片`, `视觉复核`, and `导出交接`
- [ ] visual-review desk/history are isolated to the visual-review tab
- [ ] export buttons and `Run Snapshot` are isolated to the export-handoff tab
- [ ] duplicate summary and CTA competition are removed from the overview region
</verification>

<success_criteria>

- The results workspace has explicit boundaries
- Operators no longer face one append-only result region
- Phase 5 can later modularize by workspace ownership instead of by vertical scroll order

</success_criteria>

<output>
After completion, create `.planning/phases/04.1-main-screen-information-architecture-reset/04.1-02-SUMMARY.md`
</output>
