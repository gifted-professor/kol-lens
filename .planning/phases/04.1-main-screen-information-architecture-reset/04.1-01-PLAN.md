---
phase: 04.1-main-screen-information-architecture-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.jsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "The primary operator surface is limited to intake/configuration, run launch/progress, and an explicit handoff into post-run results."
    - "RuleSpec Compiler is no longer rendered as a first-class block in the default operator workflow stack."
    - "The operator can enter a dedicated results workspace from the primary run workspace without scrolling through downstream review and export surfaces."
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Top-level workspace shell and secondary utility access for RuleSpec"
      contains: "进入结果工作台"
    - path: "frontend/src/App.jsx"
      provides: "Explicit secondary entry for the RuleSpec tool instead of an inline main-flow block"
      contains: "打开 RuleSpec 工具"
  key_links:
    - from: "frontend/src/App.jsx"
      to: ".planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md"
      via: "The screen shell must enforce the locked Phase 04.1 boundary: main workflow first, post-run workspace explicit, RuleSpec secondary."
      pattern: "进入结果工作台|打开 RuleSpec 工具|返回主工作流|RuleSpec Compiler"
---

<objective>
Establish an explicit main-screen shell so the operator-facing primary flow stops behaving like an append-only stack.

Purpose: Lock the Phase 04.1 boundary in code before reorganizing downstream result surfaces.
Output: A primary run workspace, a dedicated entry into the results workspace, and a demoted RuleSpec tool surface that no longer sits inline with the default operator flow.
</objective>

<execution_context>
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md
@README.md
@frontend/src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a top-level run/results workspace shell</name>
  <files>frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, .planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md, README.md</read_first>
  <action>In `frontend/src/App.jsx`, add an explicit top-level workspace switch using concrete values `run` and `results`. Keep upload/configuration plus the existing launch and progress surfaces in the `run` workspace. When `result` exists, render a compact result entry card in the `run` workspace with a primary control labeled exactly `进入结果工作台`, and use that entry point instead of continuing to dump review, visual-review, and export surfaces directly below the launch block. Add visible workspace copy exactly as `运行工作台` and `结果工作台` so the operator can tell which stage they are in. The `run` workspace must remain the default view before and during scrape or visual-review execution.</action>
  <verify>cd frontend && npm run lint && npm run build</verify>
  <acceptance_criteria>
    - `frontend/src/App.jsx` contains a top-level workspace state with concrete values `run` and `results`
    - `frontend/src/App.jsx` contains the exact strings `运行工作台`, `结果工作台`, and `进入结果工作台`
    - `frontend/src/App.jsx` contains both `activeWorkspace === 'run'` and `activeWorkspace === 'results'`
    - the `activeWorkspace === 'run'` branch still renders `renderJobCard('采集任务进度'` and `renderJobCard('视觉复核任务进度'`
  </acceptance_criteria>
  <done>The main operator surface has a bounded primary workspace and an explicit handoff into results.</done>
</task>

<task type="auto">
  <name>Task 2: Demote RuleSpec into an explicit secondary tool surface</name>
  <files>frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, .planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md</read_first>
  <action>Move the current `RuleSpec Compiler` block out of the default operator workflow stack. Keep the existing `templateInput`, `handleGenerateTemplates`, `templateLoading`, `templateError`, and `templateResult` behavior intact, but gate the entire RuleSpec surface behind an explicit secondary control labeled `打开 RuleSpec 工具`. Inside the tool surface, include a return control labeled `返回主工作流`. Do not leave the RuleSpec card between launch/progress and the result workspace entry in the default flow. Do not render any deprecated `审核文本转化` surface in either the `run` workspace or the `results` workspace.</action>
  <verify>cd frontend && npm run lint && npm run build</verify>
  <acceptance_criteria>
    - `frontend/src/App.jsx` contains the exact strings `打开 RuleSpec 工具` and `返回主工作流`
    - the JSX branch that renders `RuleSpec Compiler` is gated by an explicit boolean or workspace check such as `showTemplateWorkbench`
    - `frontend/src/App.jsx` contains a named tool-surface gate such as `showTemplateWorkbench` that controls the `RuleSpec Compiler` render branch
    - `frontend/src/App.jsx` does not introduce any new visible `审核文本转化` block
  </acceptance_criteria>
  <done>RuleSpec stops competing with the operator main flow and becomes an opt-in secondary tool.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
- [ ] `frontend/src/App.jsx` has an explicit `run` / `results` workspace shell
- [ ] the `run` workspace contains launch/progress and a bounded results entry instead of an append-only review/export stack
- [ ] `RuleSpec Compiler` is behind `打开 RuleSpec 工具` and no longer first-class in the default workflow
</verification>

<success_criteria>

- The primary operator flow is visibly bounded
- RuleSpec is demoted to an explicit secondary tool surface
- Operators can move from the run workspace into results without scrolling through unrelated sections

</success_criteria>

<output>
After completion, create `.planning/phases/04.1-main-screen-information-architecture-reset/04.1-01-SUMMARY.md`
</output>
