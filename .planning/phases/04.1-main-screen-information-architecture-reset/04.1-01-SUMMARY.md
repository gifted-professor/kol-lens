---
phase: 04.1-main-screen-information-architecture-reset
plan: 01
subsystem: ui
tags: [react, vite, workspace-shell, rulespec, operator-flow]
requires:
  - phase: 04-audit-ready-outputs
    provides: result/export contracts preserved while the main screen is reorganized
provides:
  - explicit run/results workspace shell on the main screen
  - bounded results entry from the primary run workflow
  - RuleSpec demoted into an explicit secondary tool surface
affects: [04.1-02, 04.1-03, 05-verification-modularization]
tech-stack:
  added: []
  patterns: [run/results workspace split, secondary tool gating for RuleSpec]
key-files:
  created: [.planning/phases/04.1-main-screen-information-architecture-reset/04.1-01-SUMMARY.md]
  modified: [frontend/src/App.jsx]
key-decisions:
  - "Keep the default operator path bounded to configuration, launch, progress, and an explicit handoff into results."
  - "Move RuleSpec Compiler behind an opt-in secondary tool entry instead of leaving it inline with the operator flow."
patterns-established:
  - "Results surfaces must be entered through explicit workspace state rather than by continuing to scroll downward."
  - "Non-core tooling such as RuleSpec stays hidden until the operator explicitly opens it."
requirements-completed: []
duration: 35min
completed: 2026-03-18
---

# Phase 04.1 Plan 01: Main Workspace Shell Summary

**The main screen now has an explicit run/results shell, a bounded entry into post-run work, and a secondary RuleSpec surface instead of an append-only operator stack**

## Performance

- **Duration:** 35 min
- **Started:** 2026-03-18T04:10:00Z
- **Completed:** 2026-03-18T04:46:58Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Added explicit `run` / `results` workspace state and visible labels `运行工作台` / `结果工作台`.
- Kept launch and job progress inside the run path while adding a compact `进入结果工作台` handoff card.
- Demoted `RuleSpec Compiler` behind `打开 RuleSpec 工具` with `返回主工作流`, removing it from the default operator stack.

## Task Commits

Plan work landed in:

1. **Task 1 + Task 2: workspace shell and RuleSpec demotion** - `77cd798` (feat)

## Files Created/Modified

- `frontend/src/App.jsx` - Added the workspace shell, results entry card, and secondary RuleSpec workbench gate.

## Decisions Made

- Kept the workspace switch on the existing screen rather than splitting into a separate route, so later modularization can still target one bounded surface.
- Preserved existing launch/progress behavior and RuleSpec compile behavior while changing only placement and ownership.

## Deviations from Plan

### Auto-fixed Issues

**1. Combined plan tasks into one commit due shared write set**
- **Found during:** Task 1 / Task 2 execution
- **Issue:** Both tasks modified the same `frontend/src/App.jsx` region, and sandbox policy blocked partial unstaging needed for clean task-by-task commit separation.
- **Fix:** Landed both Task 1 and Task 2 together in one focused UI commit.
- **Files modified:** `frontend/src/App.jsx`
- **Verification:** `cd frontend && npm run lint`, `cd frontend && npm run build`
- **Committed in:** `77cd798`

---

**Total deviations:** 1 auto-fixed
**Impact on plan:** No scope creep. The shipped boundary matches the plan; only commit granularity changed.

## Issues Encountered

- A transient `.git/index.lock` blocked one staging attempt, but the lock cleared and the commit completed successfully.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The main screen now exposes a stable run/results boundary that `04.1-02` can use for deeper result-surface ownership.
- Phase 5 can later modularize against a real workspace shell instead of the old append-only stack.

## Self-Check: PASSED

- Found `.planning/phases/04.1-main-screen-information-architecture-reset/04.1-01-SUMMARY.md` on disk.
- Verified commit `77cd798` exists in git history.

---
*Phase: 04.1-main-screen-information-architecture-reset*
*Completed: 2026-03-18*
