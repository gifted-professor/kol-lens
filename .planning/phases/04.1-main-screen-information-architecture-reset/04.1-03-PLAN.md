---
phase: 04.1-main-screen-information-architecture-reset
plan: 03
type: execute
wave: 3
depends_on:
  - 04.1-01
  - 04.1-02
files_modified:
  - README.md
  - docs/prd/master_vetting_sop.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "The new main-screen contract is documented clearly enough that later frontend work does not drift back to the old append-only structure."
    - "Maintainers can see the exact ownership split between the run workspace, results tabs, and secondary RuleSpec tooling."
    - "Phase 5 modularization guidance is anchored to the new screen boundaries rather than to the old monolithic scroll order."
  artifacts:
    - path: "README.md"
      provides: "Canonical operator-flow explanation for run workspace, results workspace, and secondary tools"
      contains: "结果工作台"
    - path: "docs/prd/master_vetting_sop.md"
      provides: "Maintainer-facing screen ownership and handoff contract"
      contains: "导出交接"
  key_links:
    - from: "README.md"
      to: "frontend/src/App.jsx"
      via: "Documentation must match the frontend's run/results workspace split and RuleSpec demotion."
      pattern: "运行工作台|结果工作台|RuleSpec"
    - from: "docs/prd/master_vetting_sop.md"
      to: ".planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md"
      via: "The SOP-level workflow description must reflect the locked ownership boundaries from Phase 04.1."
      pattern: "视觉复核|导出交接|二级工具"
---

<objective>
Lock the new main-screen information architecture into the project's canonical docs and verification routine.

Purpose: Prevent future UI work from sliding back into the old append-only layout and give Phase 5 a documented boundary model to modularize against.
Output: Updated operator and maintainer docs plus a concrete verification checklist tied to the new workspace boundaries.
</objective>

<execution_context>
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tiancaijiaoshou/Desktop/Coding/wanghong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md
@README.md
@docs/prd/master_vetting_sop.md
@frontend/src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update canonical docs to reflect the Phase 04.1 screen contract</name>
  <files>README.md, docs/prd/master_vetting_sop.md</files>
  <read_first>README.md, docs/prd/master_vetting_sop.md, .planning/phases/04.1-main-screen-information-architecture-reset/04.1-CONTEXT.md, frontend/src/App.jsx</read_first>
  <action>Update `README.md` and `docs/prd/master_vetting_sop.md` so they describe the concrete Phase 04.1 boundary model chosen in execution. In `README.md`, add the exact strings `运行工作台`, `结果工作台`, and `RuleSpec Compiler`, and state plainly that `RuleSpec Compiler` is now a secondary tool surface and not part of the default operator main flow. In `docs/prd/master_vetting_sop.md`, add the exact strings `视觉复核`, `导出交接`, and `Run Snapshot`, and document the ownership split: `运行工作台` holds intake/configuration plus launch/progress, `结果工作台` holds bounded post-run tabs, `视觉复核` owns the review desk and queue, and `导出交接` owns export actions plus `Run Snapshot`. The docs must describe the actual screen model shipped in `frontend/src/App.jsx`, not a future wish list.</action>
  <verify>rg -n "运行工作台|结果工作台|视觉复核|导出交接|RuleSpec" README.md docs/prd/master_vetting_sop.md</verify>
  <acceptance_criteria>
    - `README.md` contains the exact strings `运行工作台` and `结果工作台`
    - `README.md` contains both `RuleSpec Compiler` and `secondary tool`
    - `docs/prd/master_vetting_sop.md` contains the exact strings `视觉复核` and `导出交接`
    - `docs/prd/master_vetting_sop.md` contains both `Run Snapshot` and `secondary audit context`
  </acceptance_criteria>
  <done>The repository's canonical docs now lock the new screen ownership model.</done>
</task>

<task type="auto">
  <name>Task 2: Add a concrete Phase 04.1 verification checklist for the new boundaries</name>
  <files>README.md, docs/prd/master_vetting_sop.md</files>
  <read_first>README.md, docs/prd/master_vetting_sop.md, frontend/src/App.jsx</read_first>
  <action>In the same docs update, add a concrete verification checklist for the shipped Phase 04.1 screen boundary. Put that checklist in `README.md` and include the exact strings `进入结果工作台`, `概览`, `导出交接`, `cd frontend && npm run lint`, and `cd frontend && npm run build`. The checklist must state that default landing opens on `运行工作台`, `进入结果工作台` opens the results surface, `RuleSpec Compiler` requires the explicit secondary entry, `概览` does not contain the export wall or visual-review desk, `视觉复核` owns the desk and queue, and `导出交接` owns export actions plus `Run Snapshot`.</action>
  <verify>rg -n "进入结果工作台|概览|导出交接|npm run lint|npm run build" README.md docs/prd/master_vetting_sop.md</verify>
  <acceptance_criteria>
    - `README.md` contains the exact string `进入结果工作台`
    - `README.md` contains the exact strings `概览` and `导出交接`
    - `README.md` contains `cd frontend && npm run lint`
    - `README.md` contains `cd frontend && npm run build`
  </acceptance_criteria>
  <done>The new screen contract is backed by a repeatable verification checklist instead of memory.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "运行工作台|结果工作台|视觉复核|导出交接|RuleSpec" README.md docs/prd/master_vetting_sop.md`
- [ ] `rg -n "进入结果工作台|概览|cd frontend && npm run lint|cd frontend && npm run build" README.md docs/prd/master_vetting_sop.md`
- [ ] docs describe the shipped screen ownership model rather than the old append-only layout
- [ ] docs give Phase 5 a concrete modularization boundary to follow
</verification>

<success_criteria>

- The new screen contract is documented in canonical project docs
- Maintainers can see the ownership split without reverse-engineering `App.jsx`
- Later UI work has an explicit verification checklist tied to the Phase 04.1 boundaries

</success_criteria>

<output>
After completion, create `.planning/phases/04.1-main-screen-information-architecture-reset/04.1-03-SUMMARY.md`
</output>
